import React from "react";
import ProgressSwiper from "./Slider";

function Hero() {
	return (
		<div className="hero">
			<div className="container-box">
				<ProgressSwiper />
			</div>
		</div>
	);
}

export default Hero;
